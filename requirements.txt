# Clinical Physiology Calculator - Requirements
# Core dependencies for the web UI
flask>=2.3.0

# The CLI version uses only Python standard library, but the UI requires flask.

# Recommended development tools:
# For testing (optional):
pytest>=7.4.0

# For code formatting and linting (optional):
black>=23.7.0
flake8>=6.0.0

flask>=2.3.0
flask-sqlalchemy>=3.0.0
flask-login>=0.6.3
reportlab>=4.0.0

# To use this project:
# 1. Ensure Python 3.7+ is installed
# 2. Install packages: pip install -r requirements.txt
# 3. Run: python app.py
